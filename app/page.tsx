import Hero from "@/components/Hero";

export default function Home() {
  return (
    <main>
      <Hero />
      {/* Scroll 2 — How It Works   (Entrega 2) */}
      {/* Scroll 3 — Choose Your Path (Entrega 3) */}
      {/* Scroll 4 — Forms            (Entrega 4) */}
    </main>
  );
}
